# Fama deep audit — 2026-08-11

This is the shared engineering record for Codex, Claude, and the maintainer.
It now covers `master` through `e1831bd`, including the architecture follow-up,
Codex restart fix, and public `v0.12.5` release. Update this file rather than
creating a competing handoff note.

## Outcome

No known critical or high-severity dependency vulnerability remains. After the
five-Module architecture follow-up and Codex restart fix, the source suite passes 94/94 tests and
the measured first-party test set has 92.22% aggregate line coverage. Every JavaScript file parses, a clean isolated Windows package passes
the release secret gate, and a rendered browser check passes with no console
errors. No real credential pattern was found in the working tree or full Git
patch history.

The architecture follow-up adds 16 first-party files: the domain context and
ADR, three server-domain Modules, five Desktop Runtime Modules, five focused
test files, and the tagged-release workflow.

The audit reviewed every tracked first-party source and documentation file plus
the in-scope files added by the combined Claude/Codex work. Generated
dependencies and existing packaged output were audited via their manifests,
dependency scanner, and a fresh isolated package rather than treated as
first-party source.

## Changes made

### Security and privacy

- Added a strict same-origin Content Security Policy and standard browser
  hardening headers to every local HTTP response.
- Kept the random mutation/spend token same-origin and no-store through
  `/auth.js`; static HTML no longer needs inline token injection.
- Hardened decoded static-path containment, query-string handling, `HEAD`, and
  method rejection. Existing loopback binding, Host allowlist, body limit, and
  mutation-token checks remain required.
- Restricted transcript inline images to PNG/JPEG/GIF/WebP. SVG and unsafe URL
  schemes are discarded. HTTPS image URLs render as explicit links and are
  never fetched automatically.
- Removed the Google Fonts request from the local application. With cloud voice
  disabled, the desktop update check is the only routine outbound app request
  and it contains no transcript content.
- Redacted common API tokens and private keys before cloud narration and from
  client/provider error logs. Removed the spoken-text response header.
- Removed inline styles and dynamic style mutation so the local UI now enforces
  a self-only style policy without `unsafe-inline`.
- Added 30-second provider timeouts, strict speech kind/speed normalization,
  bounded transcript reads, a bounded partial-line buffer, and a two-megabyte
  backlog byte ceiling.
- Hardened Electron with sandboxing, no renderer Node integration/webviews,
  denied permissions/downloads, HTTPS-only external navigation, IPC sender
  checks, validated project identifiers, safe shortcut creation, and per-user
  config permissions.
- The packaged app now launches its server with Electron's bundled runtime via
  `process.execPath` and `ELECTRON_RUN_AS_NODE=1`, removing the PATH hijack and
  separate-Node prerequisite.
- Broadened package and repository secret exclusions, added Google and
  fine-grained GitHub token scanning, and made the package verifier accept an
  isolated audit directory.

### Reliability, usability, and accessibility

- `Narrator.stop()` now invalidates queued speech, aborts cloud work, settles
  current audio, revokes blob URLs, and cannot replay stale text through local
  fallback.
- Settings now behaves as a labeled modal: focus enters predictably, Tab stays
  inside, Escape closes it, and focus returns to the opener. Status/key/lane
  controls expose appropriate live, expanded, pressed, and label semantics.
- A failed Settings save no longer triggers a misleading voice test.
- Closed a transcript file-descriptor leak and made desktop-shortcut success
  state reflect the actual write result.
- Enforced the documented global idle-notification cooldown and moved Session
  debounce, threshold, provider-label, error, and cleanup policy behind the
  tested Desktop Notifications Interface.
- Preserved and tested the pre-existing Dominican-English preset and respectful
  prompt expansion from Claude's working tree.
- Preserved a browsed Selected Project's canonical `cwd` across desktop
  restarts. This closes the real failure where a Codex-only folder was saved
  correctly but reduced to a Claude transcript identifier during startup,
  preventing Codex session matching after restart.
- Added the Signal Aperture identity across desktop/tray, onboarding, live
  status, and empty-state surfaces, backed by one documented source asset and
  optimized runtime derivatives.
- Added a Claude-like Messages projection with readable prompts/replies and
  expandable reasoning/tool/result cards. Activity preserves the compact
  technical projection of the identical live-only event stream. View choice
  persists locally, narrow layouts avoid horizontal overflow, controls retain
  keyboard operation, and all motion respects reduced-motion preferences.

### Dependencies and delivery

- Pinned Electron `43.3.0`, electron-builder `26.15.3`, electron-packager
  `17.1.2`, and electron-updater `6.8.9`. Both full and production-only
  `npm audit` now report zero vulnerabilities.
- Electron 39 was briefly selected during the shared pass but was already
  end-of-life on 2026-05-05. The final audit uses the supported Electron 43
  line, whose published support window runs through 2027-01-05.
- CI uses deterministic `npm ci`, read-only default permissions, cancellation
  of superseded runs, job timeouts, a high-severity audit gate, and a blocking
  installer build. Third-party actions are pinned to reviewed commit SHAs on
  their current Node 24-based v7 releases. Node 18 installs production
  dependencies only; the full Electron toolchain runs on Node 22.
- Added weekly Dependabot checks for npm and GitHub Actions.
- Added Windows/macOS test smoke jobs and Linux/macOS portable-package gates.
  Tagged Windows releases now test, audit, build, secret-scan, and publish the
  NSIS update metadata only after every gate passes.
- Replaced automatic update download/install behavior with a tested Update
  Runtime that asks before downloading and asks again before restarting.
- Portable packages without installer metadata now skip automatic update checks
  cleanly, while a manual check offers the Releases page.
- Kept package-secret scanning over text and ASAR content while excluding
  extensionless ELF/Mach-O executables from UTF-8 regex matching. This resolves
  confirmed Linux/macOS binary false positives without weakening the original
  `.env`, forbidden-path, or packaged-application content gates.
- Made HTTP/SSE integration tests allocate an ephemeral port and await child
  shutdown, eliminating cross-talk between simultaneous Claude/Codex audit runs.
- Rechecked the exact official OpenAI catalog entry and model page. The catalog
  lists `gpt-4o-mini-tts` without a deprecation badge and its detail page labels
  it "Default." An adjacent GPT-Realtime mini badge was briefly misattributed
  during concurrent work and is corrected here. Fama keeps mini-TTS as the
  style-capable default and retains `tts-1` as the supported fallback. Pricing
  and the display-ready model/voice/capability catalog are owned by Cloud
  Narration and supplied to the viewer instead of duplicated.
- Published `v0.12.5` from merge commit `e1831bd` with a CI-built NSIS
  installer, verified portable ZIP, and SHA-512-matched updater manifest.

### Cross-agent handoff

- `AGENTS.md` remains the shared operating policy and points both agents to
  this audit record.
- Root `CLAUDE.md` now directs Claude to the same policy, domain context, and
  handoff record that Codex uses.
- Fama is deliberately an observer, not a hidden-memory bridge. Claude and
  Codex coordinate through committed code, PR history, and this durable record;
  neither agent is told that private reasoning or undeclared state was shared.

## Verification evidence

| Check | Result |
| --- | --- |
| `npm test` | 94 passed, 0 failed |
| First-party coverage after architecture follow-up | 92.22% lines, 73.85% branches, 81.76% functions |
| Pre-follow-up core library coverage | 93.33% lines, 75.66% branches, 93.55% functions |
| Package verifier coverage | 89.81% lines, 68.75% branches, 100% functions |
| JavaScript syntax | 43 files passed `node --check` |
| `npm audit --omit=dev --audit-level=high` | 0 vulnerabilities |
| `npm audit --audit-level=high` | 0 vulnerabilities |
| Dependency tree | Electron 43.3.0 / builder 26.15.3 / packager 17.1.2 / updater 6.8.9 |
| Working-tree secret scan | only deliberate fake credential fixtures in `test/redact.test.js` and `test/cloud-narration.test.js` |
| Git patch-history secret scan | 0 credential matches |
| Isolated Windows package | Electron 43.3.0 package completed; `verify-package.js` passed |
| Windows NSIS installer | `Fama-Setup-0.12.5.exe` built in post-merge CI; portable package passed the secret gate |
| Real Codex live feed | Current Codex desktop session appeared as provider `codex`, project `Documents`, with its expected session ID after a Fama restart |
| GitHub delivery | PR #3 merged; all 8 pre-merge and all 8 post-merge jobs passed; `v0.12.5` is latest |
| Packaged bundled runtime | Electron 43.3.0 ran as Node 24.18.1 without PATH Node |
| Packaged visual identity | `viewer/identity-signal.png` present in the electron-builder ASAR and portable package |
| Rendered UI | meaningful page, expected controls, 0 console errors |
| Messages / Activity views | complete prompt/reply flow rendered; reasoning expanded to full carried text; persisted view switch passed |
| Responsive viewer | 700×900 and 420×820 checks passed with no horizontal overflow; settings stayed within viewport |
| Session keyboard flow | Enter collapsed and Space expanded the session; `aria-expanded` reflected both states |
| Settings keyboard flow | modal label/focus passed; Escape returned focus to Settings |
| Whitespace/error scan | `git diff --check` passed |

## Open risks and boundaries

These are not hidden failures and do not block this audit commit:

- Windows artifacts are still unsigned. A code-signing certificate or
  Microsoft Trusted Signing enrollment requires owner identity verification
  and payment; engineering cannot manufacture that trust signal.
- Real macOS/Linux hardware behavior remains unverified, although CI now proves
  Node/server behavior on Windows and macOS plus portable packaging on Linux
  and macOS.
- The tagged release publication path has run end to end for `v0.12.5` with
  verified CI artifacts. Applying the update from an older installed build and
  confirming the post-restart version still requires a real installer run.
- Tray, shortcut, and real BrowserWindow reuse behavior still relies more on
  package/browser/manual checks than unit automation. Config, child-server,
  updater, and Desktop Notifications lifecycle, permission/navigation policy,
  and IPC sender checks now have isolated tests.
- OpenAI cost figures are explicitly estimates. The official pages were checked
  on 2026-08-11; mini-TTS is token-billed and approximated,
  while the `tts-1`/`tts-1-hd` rates are exact per-character provider rates.

## Architecture exploration candidates (implemented follow-up)

The audit kept these refactors out of the security patch. The maintainer then
selected all five for a separate architecture branch. They are now implemented
behind the glossary and invariants in `CONTEXT.md`, with the decision recorded
in `docs/adr/0001-deepen-runtime-modules.md`; the original problem/solution
notes remain below as the shared rationale.

1. **Deepen Live Activity Ingest Module**

   **Problem:** provider discovery, session identity, polling, tailer lifecycle,
   normalization, and broadcast orchestration are spread through `server.js`.
   Codex session discovery recursively scans on a timer, while `FileTailer`
   exposes offset/buffer mechanics to orchestration policy.

   **Solution:** create one ingest module that owns provider discovery cadence,
   selected-project matching, tailer lifecycle, and normalized live events.
   Keep Claude/Codex adapters as concrete seams, not a generic filesystem layer.

   **Benefits:** easier provider additions, targeted performance work, and one
   place to prove live-only/no-cross-project semantics.

   **Tests:** provider discovery fixtures, create/rotate/truncate lifecycle,
   project isolation, polling backoff, and normalized event contract tests.

2. **Deepen Selected Projects Module**

   **Problem:** encoded Claude directory names, real working directories,
   labels, and positional environment arrays form one concept but live across
   onboarding, config, Electron, and server startup. A manually browsed real
   path is reduced to an encoded name, and identity is lossy/positional.

   **Solution:** persist a canonical selected-project record with stable ID,
   real path, optional Claude transcript directory, and label; serialize that
   record through one boundary.

   **Benefits:** reliable names, safer migration, simpler Codex matching, and
   fewer parallel arrays that can drift.

   **Tests:** config migration, hyphenated/cross-platform paths, deduplication,
   browse flow preservation, and stable identity after reorder/restart.

3. **Deepen Cloud Narration Module**

   **Problem:** provider calls, rewrite policy, voice-style expansion, cost
   estimates, usage accounting, redaction, and HTTP route handling are spread
   across server/viewer files, with pricing knowledge duplicated.

   **Solution:** give one server-side narration module ownership of validated
   config, rewrite/TTS policy, provider errors, usage estimates, and results.

   **Benefits:** a smaller secret-bearing surface, one source of cost truth,
   and a clean seam for future local or alternate providers.

   **Tests:** no-rewrite threshold, provider timeout/fallback, redaction,
   supported instructions, usage accounting, and model-specific estimates.

4. **Deepen Narrator Module**

   **Problem:** browser narration policy is split between `app.js`,
   `settings.js`, and mutable `Narrator` internals. Cancellation, queue limits,
   provider fallback, and UI state can drift.

   **Solution:** expose a small intent API (`configure`, `enqueue`, `stop`,
   `status`) and keep generation/queue/audio/fallback state private.

   **Benefits:** fewer stale-audio races and direct, deterministic tests of the
   behavior users hear.

   **Tests:** enable/disable transitions, pinned-session policy, queue pressure,
   abort during each phase, fallback once, and state callbacks.

5. **Deepen Desktop Runtime Module**

   **Problem:** `desktop/main.js` is a side-effectful composition root for
   config, migrations, process lifecycle, windows, tray, updates,
   notifications, shortcuts, project selection, and IPC.

   **Solution:** retain a thin composition root and deepen existing concepts
   into desktop config, local-server lifecycle, window policy, and update
   modules. Avoid route-per-file or generic platform abstractions.

   **Benefits:** safer Electron upgrades, mockable lifecycle tests, and clearer
   ownership of security policy.

   **Tests:** server start/stop/error, allowed navigation, IPC sender rejection,
   config migration/permissions, window reuse, and update result handling.

Rejected superficial splits: route-per-file extraction, merging provider
parsers, a generic filesystem interface, centralizing package ignores without
an independent verifier, and splitting tiny leaf modules. Those moves add
indirection without deepening a coherent responsibility.

## Non-negotiable invariants for the next agent

- Keep the server bound to loopback and retain Host plus mutation-token checks.
- Never send transcript content anywhere except the explicitly enabled voice
  provider; never auto-load transcript-supplied remote media.
- Never echo, log, commit, or package a real API key. The artifact scan must be
  a release blocker.
- Preserve provider/session/project attribution and live-only tailing; do not
  replay historical transcripts on startup.
- Preserve the user's existing changes. Check `git status` and the shared audit
  before editing, then update this record with verification evidence.

## Primary references checked

- OpenAI TTS-1 documentation: https://developers.openai.com/api/docs/models/tts-1
- OpenAI TTS-1 HD documentation: https://developers.openai.com/api/docs/models/tts-1-hd
- OpenAI model catalog (current availability status): https://developers.openai.com/api/docs/models
- OpenAI mini TTS documentation: https://developers.openai.com/api/docs/models/gpt-4o-mini-tts
- OpenAI rewrite-model documentation: https://developers.openai.com/api/docs/models/gpt-4o-mini
- Electron 43 release notes: https://www.electronjs.org/blog/electron-43-0
- Electron release/support schedule: https://releases.electronjs.org/schedule
- Electron planned breaking changes: https://www.electronjs.org/docs/latest/breaking-changes
